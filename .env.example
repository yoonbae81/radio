# Radio Recording Service Environment Variables
# Copy this file to .env and configure your programs
# DATA_DIR is the host directory for recordings and logos
DATA_DIR=/srv/radio

# Secret for feed authentication (optional, leave empty to disable auth)
# Support multiple secrets: SECRET=secret1,secret2,secret3
SECRET=

# Port for feed service (default: 8013)
PORT=8013

# Route prefix for feed URLs (default: /radio)
ROUTE_PREFIX=/radio

# Cache TTL in seconds (default: 3600 = 1 hour)
CACHE_TTL=3600

# Force HTTPS in feed URLs (useful if service is behind a proxy)
FORCE_HTTPS=false

# Global stream URL
STREAM_URL=https://example.com/stream.m3u8

# Program Configuration
# Format: PROGRAM1=start-end|alias|name|stream_url
# - start-end: Time range in HH:MM-HH:MM format
# - alias: URL-friendly identifier (used in feed URLs and logo filename: logo/alias.png, .jpg, .jpeg)
# - name: Full program name
# - stream_url: The radio stream URL for this specific program
# Note: For multiple time slots, create separate PROGRAM entries

PROGRAM1=07:40-08:00|program1|Program Name #1
PROGRAM2=08:00-08:20|program2|Program Name #2|https://other-stream-url.com/stream2.m3u8

